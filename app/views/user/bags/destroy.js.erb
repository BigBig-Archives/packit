<% if @bag.errors.any? %>
  console.log("errors: <%= j @bag.errors.full_messages.join('; ') %>")
<% else %>
  content = document.querySelector("[data-bag=\"<%= @bag.id %>\"]");
  content.parentNode.removeChild(content);
<% end %>
