function updateItem() {
  console.log("update item");

  let item;
  let container;
  let content;

  <% if @item.errors.any? %>
    console.log("error(s)");
    item = document.querySelector(`[data-item=\"<%= j @item.id.to_s %>\"]`);
    <% @item.errors.messages.each do |key, value| %>
      container = item.querySelector("[data-error=\"<%= j key.to_s %>\"]");
      content = `<%= j key.to_s %> <%= j value.first.to_s %>` ;
      container.innerHTML = content;
    <% end %>
  <% else %>
    let container_items_created = document.querySelector("#items-created");
    let container_items_to_pack = document.querySelector("#items-to_pack");
    if (container_items_created)
    content = `<%= j render 'user/packed_bags/cards/display_items_created' %>`;
    container.innerHTML = content;
    console.log('a');
    // Refresh load
    let load_container = document.querySelector("#navbar #load");
    console.log(load_container);
    let load = `<%= j render 'user/packed_bags/navbar/load' %>`;
    // load_container.innerHTML = load;
  <% end %>
}

updateItem();
