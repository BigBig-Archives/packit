<h1>User:Items</h1>

<ul>
  <% @items.each do |item| %>
    <%= render 'shared/item_overview',
      type: 'owned',
      item: item,
      commentary: item.commentary
    %>
  <% end %>
</ul>

<%= render 'shared/back_list' %>
