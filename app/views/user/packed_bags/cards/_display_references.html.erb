<div id="references">
  <p class="cards-indication">Here are all the items you can create</p>
  <hr>
  <div class="items-container">
    <% @references.each do |reference| %>
      <%= render 'item_references/reference', reference: reference %>
    <% end %>
  </div>
</div>
