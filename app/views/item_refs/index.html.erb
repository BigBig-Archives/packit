<div class="container">
  <nav class="nav nav-pills" id="filter-by-category">
    <%= render 'filter_by_category',
      categories: @categories,
      filtered_by_category: @filtered_by_category,
      category_selected: @category_selected,
      filtered_by_owned: @filtered_by_owned,
      owned_selected: @owned_selected
    %>
  </nav>
  <nav class="nav nav-pills" id="filter-by-owned">
    <%= render 'filter_by_owned',
    category_selected_id: @category_selected_id,
    filtered_by_category: @filtered_by_category,
    category_selected: @category_selected,
    filtered_by_owned: @filtered_by_owned,
    owned_selected: @owned_selected
  %>
  </nav>
  <div class="item-results" id="results">
    <%= render 'results',
      categories: @categories,
      owned_references: @owned_references,
      filtered_by_category: @filtered_by_category,
      filtered_by_owned: @filtered_by_owned,
      category_selected: @category_selected,
      owned_selected: @owned_selected
    %>
  </div>
</div>
