<%= link_to user_packed_bag_path(@packed_bag, params: { owned: false,  category: params[:category] }), class: "nav-link", remote: :true do %>
  all items
<% end %>

<%= link_to user_packed_bag_path(@packed_bag, params: { owned: true,  category: params[:category] }), class: "nav-link", remote: :true do %>
  items used in previous packed bags
<% end %>
