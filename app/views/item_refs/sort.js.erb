itemResultsContainer = document.querySelector('.item-results');
itemResults = `<%= j render 'results', categories_displayed: @categories_displayed %>`;
itemResultsContainer.innerHTML = itemResults;

itemNavContainer = document.querySelector('nav');
itemNavElts = `<%= j render 'nav', categories: @categories, filter: @filter %>`;
itemNavContainer.innerHTML = itemNavElts;
