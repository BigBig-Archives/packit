<h1>BagRefs#index</h1>

<ul>
  <% @bag_refs.each do |bag_ref| %>
    <%= render 'shared/bag_overview',
      path: bag_ref_path(bag_ref),
      name: bag_ref.name,
      size: bag_ref.size
    %>
  <% end %>
</ul>
